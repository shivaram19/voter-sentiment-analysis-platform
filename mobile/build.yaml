targets:
  $default:
    builders:
      drift_dev:
        options:
          store_date_time_values_as_text: true
      json_serializable:
        options:
          explicit_to_json: true
      freezed:
        options:
          # Freezed generates immutable data classes for domain models.
          # Run: flutter pub run build_runner build --delete-conflicting-outputs
          {} 
