import '../../../core/network/api_client.dart';

class SyncApi {
  final ApiClient _client;

  SyncApi(this._client);

  // Batch sync is implemented in SurveyApi; this class reserved for future sync endpoints.
}
